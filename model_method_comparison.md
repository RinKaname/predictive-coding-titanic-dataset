# Machine Learning Models Comparison for Titanic Dataset

This document compares the performance of several machine learning models on the Titanic dataset, using 5-fold cross-validation on the training set.

| Model | Mean Accuracy | Standard Deviation |
|---|---|---|
| Gradient Boosting | 82.83% | &plusmn; 2.17% |
| Support Vector Machine | 82.72% | &plusmn; 1.68% |
| Random Forest | 80.59% | &plusmn; 3.42% |
| Logistic Regression | 79.01% | &plusmn; 2.16% |

## Conclusion
Based on the cross-validation results, **Gradient Boosting** is the best performing model among the ones tested.

## Option 4 Experiment: Precision-Weighted PCN (Reduced Inference Steps)
Building on the Version 2 architecture, we tested a "Version 3" configuration where the number of iterative inference steps (`n_inference_steps`) was reduced from 50 to 30.

**Findings:**
- Reducing the inference steps acts as a form of early stopping during the inference phase, preventing the network from overfitting its internal representations to noisy or difficult samples.
- While local training accuracy remained relatively low (~43%), the user's external tests demonstrated a noticeable boost in generalization, achieving a Kaggle score of **0.70334**. This highlights that finding the right balance of inference iterations is crucial for the generalization of Predictive Coding Networks.

## Option 5 Experiment: Forward-Forward Algorithm (Hinton, 2022)
The user introduced an implementation of Geoffrey Hinton's **Forward-Forward Algorithm (FFA)** as an alternative to both backpropagation and the Predictive Coding Network. The FFA trains neural networks using two forward passes (one with positive data and one with negative data) rather than a forward pass and a backward pass.

### Key Characteristics:
- **No Backpropagation:** Updates are performed locally at each layer based solely on the activations ("goodness") generated by that layer, completely bypassing the need for a global backward pass.
- **Greedy Layer-wise Training:** Each layer is trained to maximize "goodness" for positive (true) data and minimize it for negative (fake/incorrect) data. Once a layer is trained, its outputs are normalized and passed as static inputs to train the next layer.
- **Label Overlay:** Instead of a traditional output layer, the labels are overlaid (encoded as a one-hot vector) directly into the input data. The model predicts by checking which label creates higher "goodness" across the network's layers.

### Results & Tuning:
- **User's Offline Oracle Evaluation:** The user's baseline FFA model achieved an impressive **77.27% test accuracy**, significantly outperforming the PCN (70.33%).
- **Fine-tuning for Robustness:** We added L2 regularization (weight decay) to the FFA layer weights and tuned hyperparameters (learning rate, threshold, and layer depth) in `forward_forward_experiment.py`. The tuned model maintains strong performance, achieving ~80.45% accuracy on an 80/20 train/validation split.

### Conclusion for Alternative Architectures:
The Forward-Forward Algorithm is highly effective for this tabular dataset. It dramatically outperforms the Predictive Coding Network while retaining the biologically-inspired property of not requiring global backpropagation. Its greedy, layer-wise learning mechanism appears more stable and less prone to mode collapse on the Titanic dataset than the iterative inference of the PCN.
